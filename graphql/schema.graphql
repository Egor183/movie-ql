type Movie {
  name: String!
  score: Float!
  id: Int!
}

type Query {
  movies: [Movie]!
  movie(id: Int!): Movie
}

type Mutation {
  addMovie(name: String!, score: Float!): Movie
  deleteMovie(id: Int!): Boolean
}
